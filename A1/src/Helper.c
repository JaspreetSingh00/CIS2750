#include <stdio.h>
#include "Helper.h"
#include "SVGParser.h"
#include "LinkedListAPI.h"

// SVGimage * new_svgimage()
// {
//     SVGimage *image = malloc(sizeof(SVGimage));

//     char * (*printCircle)(void *) = &circleToString;

//     image->circles = initializeList(printCircle, )
// }

// char* createNewString(void)
// {
// 	char* newString = calloc(50, sizeof(char));
	
// 	return newString;
// }